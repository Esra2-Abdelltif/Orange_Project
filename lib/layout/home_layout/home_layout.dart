import 'package:flutter/material.dart';

class HomeLoyout extends StatelessWidget {
  const HomeLoyout({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
